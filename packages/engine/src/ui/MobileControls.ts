/**
 * Rift & Raid — MobileControls
 *
 * Wraps VirtualJoystick for HUD integration. Phase 0 delegates to
 * VirtualJoystick directly; Phase 8 will add proper UI polish.
 */

export class MobileControls {
  // Phase 0: VirtualJoystick handles its own DOM creation in attach().
  // This class is a placeholder for Phase 8 polish (responsive layouts,
  // customizable button positions, haptic feedback).
}
