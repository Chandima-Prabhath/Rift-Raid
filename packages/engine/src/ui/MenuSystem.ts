/**
 * Rift & Raid — MenuSystem
 *
 * Phase 0 stub. Pre-game menus (team select, class select, loadout) come
 * in Phase 6+. Phase 0 just boots into the prototype scene directly.
 */

export class MenuSystem {
  // Phase 6+
}
